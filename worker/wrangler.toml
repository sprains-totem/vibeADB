name = "vibeadb-mailbox"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# 部署前执行 `npx wrangler kv namespace create KV`，
# 把返回的 id 填到下面（详见 DEPLOY.md）。
[[kv_namespaces]]
binding = "KV"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

# 写令牌不放 wrangler.toml：
#   npx wrangler secret put WRITE_TOKEN
