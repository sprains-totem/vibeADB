{
  "name": "vibeadb-mailbox",
  "version": "0.1.0",
  "private": true,
  "description": "vibeADB URL mailbox (deviceId -> current tunnel domain)",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
