{
  "name": "vibeadb-relay",
  "version": "2.0.0",
  "private": true,
  "description": "vibeADB Durable Object edge relay (device leg <-> client leg pairing pipe)",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
