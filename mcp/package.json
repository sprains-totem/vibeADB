{
  "name": "vibeadb-mcp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "vibeADB MCP server — AI Agent 通过原生 MCP 工具调用真机（Shizuku）能力",
  "bin": {
    "vibeadb-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
